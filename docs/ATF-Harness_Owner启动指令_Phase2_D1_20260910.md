# Owner 启动指令：Phase 2 / D1（ACP 消费面设计定型）

> 日期：2026-09-10 ｜ 签发：owner ｜ 执行方：zcode
> 依据：《ATF独立Harness_Phase2任务书_20260910.md》§1（D1 设计要求与验收标准）+ 《ATF-Harness_Owner决议_Phase2范围确认与任务书签发_20260910.md》§2（启动指令）+ 《ATF-Harness_P2S2审批应答语义设计草案_20260910.md》§5（schema 结论）+ AGENTS.md 常驻硬约束 + Phase 1 已闭合（tag `v0.1.0`，109 passed / 2 skipped，pin `v0.2.0b7` / `a628f8b`）。
> 结论先行：**D1 现在启动。只做 D1（文档产出，不写代码），完成即停——P2-S1 未获指令不得启动。**

---

## 1. 范围

- 产出《ATF独立Harness_ADR-09候选_ACP消费面定型_20260910.md》，覆盖任务书 §1 设计要求 1–4，五节内容缺一不可：
  1. dispatch / resume 的调用形态；
  2. 会话事件向宿主的投影白名单；
  3. 审批请求经宿主转达与应答回流的形态；
  4. provider 配置的注入来源（宿主注入 vs harness 自管）；
  5. 多轮审批会话如何透出。
- **必须吸收**《P2S2 审批应答语义设计草案》§5 的 schema 结论：审批会话模型（`approval_session_id` ↔ `tool_call_id` 配对）、统一 `approval/response` + `verdict` 枚举（六类：granted / advised / denied / aborted / clarification / timeout）、`supersedes` 提案演化链、`actor` 应答者身份。
- 文档须含「Phase 2 不实现项清单」，与任务书 §5 条件项登记一致（ACP server 本体、TUI/Web 界面、`projection` 激活、闸 B、re-pin）。

## 2. owner 预先拍板的口径（撰写时直接采用，无需再问）

| # | 事项 | 口径 |
|---|---|---|
| 1 | 文档形态 | 中文，结论先行；结构 = 五节设计 + 接口草案 + 不实现项清单 + 与 Phase 3 的边界声明。命名 `ATF独立Harness_ADR-09候选_ACP消费面定型_20260910.md` |
| 2 | 抽象层级 | 只定**消费面形态**（调用载荷、事件投影、审批往返、配置注入、resume 语义），**不定传输层协议与实现选型**（JSON-RPC 帧格式、进程模型、鉴权机制均属 Phase 3） |
| 3 | 事件投影口径 | 沿用 S5 决议① ——工作区动作不落会话事件，白名单不扩；新增审批会话类事件按决策六 C 口径（统一 `approval/response` + `verdict`） |
| 4 | provider 配置注入 | 文档中**并列**两种模式（宿主注入 / harness 自管）并给出取舍建议，最终由 owner 裁决——不要自行定案 |
| 5 | 调研边界 | 可查阅公开规范（如 ACP 已有实现形态）作为参考，但**不得引入任何依赖、不得写原型代码**（含临时试验目录） |
| 6 | 冲突处置 | 若 D1 结论要求调整 P2-S1/S2/S3 的条款，提**条款级修订建议**（列出原条款、建议改法、理由），不改规划文档、不自行实现 |

## 3. 执行序列

1. 阅读四份 Phase 2 文档（规划 / 决议 / 任务书 / 应答语义设计草案）+ 本指令；与本指令冲突时以本指令为准，并报告差异。
2. 撰写 D1 文档；自检清单：五节是否齐备、schema 结论是否吸收、不实现项与边界是否声明、是否越界到传输层。
3. 产出《Phase2_D1执行报告_20260910.md》（执行记录 / 五节对照 / 偏离与决策点 / 提交清单）。
4. **文档入库（本指令随附动作）**：将随本指令一并投放到 `docs/` 的 Phase 2 文档提交入库——《规划设计》《Owner决议_Phase2范围确认与任务书签发》《Phase2任务书》《P2S2审批应答语义设计草案》+ 本启动指令，共 5 份。建议提交信息：`docs(owner): Phase 2 范围决议与任务书入库 + D1 启动指令`。
5. **提交本地保存，不 push**——push 与否待 owner review 后决议（沿用 Phase 1 纪律）。

## 4. 纪律不变条款

1. **纯文档切片**：不改 `src/`、不改契约 yaml、不新增依赖、不写任何实现代码。
2. 测试基线 109 passed / 2 skipped 不得回归（D1 不改代码，复跑应完全一致；如复跑请附输出）。
3. 内核仓只读：pin `v0.2.0b7` / `a628f8b` 不动；re-pin 未获 C1 专项指令前不得开展。
4. 无 GPU、无真实 LLM Provider、无网络模型调用；`dependencies` 保持为空。
5. 条件项（P2-S4 晋升闸 B、C1 re-pin）未触发前，不得开展相关探索性调研。
6. 未获 P2-S1 启动指令不得进入 P2-S1；D1 完成即停，等 owner review。
